# Fedora 44 GNOME Customization Pack

This repository contains a curated collection of configuration files, themes, icons, fonts, shell settings, and GNOME preferences from my Fedora 44 GNOME desktop environment. 

It is structured to help you easily replicate this setup on your own Fedora system.


## 📂 Structure & Contents

* **`dconf/`**: GNOME and Extension Settings
  * `gnome-settings.dconf`: Complete export of all GNOME settings, keyboard shortcuts, window manager tweaks, and extension profiles.
  * `openbar-settings.dconf`: Dedicated export of the OpenBar top bar extension settings.
* **`gtk/`**:
  * `gtk-3.0/`: Custom GTK 3 stylesheets (`gtk.css`, `gtk-dark.css`) and settings.
  * `gtk-4.0/`: Custom GTK 4 stylesheets and settings.
* **`extensions/`**: Copied GNOME Shell extension files (see full list below).
* **`themes/`**: Custom GTK and Shell themes (e.g., Catppuccin Macchiato).
* **`icons/`**:
  * `local-icons/`: Custom cursor and icon sets (e.g., Bibata, Flat Remix) from `~/.icons/`.
  * `share-icons/`: Icon sets from `~/.local/share/icons/`.
* **`fonts/`**: User-specific fonts and Nerd Fonts (e.g., JetBrainsMono, CascadiaCode) from `~/.local/share/fonts/`.
* **`shell/`**:
  * Shell files including `bashrc`, `zshrc`, `fish` config, `starship.toml` configuration, and `ohmyposh` settings.
* **`other-configs/`**:
  * Configurations for helper utilities like `fastfetch`, `Kvantum` (Qt theme integration), `qt5ct`, and `qt6ct`.


## 📦 Installed GNOME Shell Extensions Included
* **Appearance Keeper** (`appearance-keeper@melomane13`)
* **ArcMenu** (`arcmenu@arcmenu.com` & `arc-menu@linxgem33.com`)
* **A-Z Clock** (`azclock@azclock.gitlab.com`)
* **Blur my Shell** (`blur-my-shell@aunetx`)
* **Caffeine** (`caffeine@patapon.info`)
* **Clipboard History** (`clipboard-history@alexsaveau.dev`)
* **Color Picker** (`color-picker@tuberry`)
* **Compiz Windows Effect** (`compiz-windows-effect@hermes83.github.com`)
* **Dash to Dock Lite** (`dash2dock-lite@icedman.github.com`)
* **Dash to Dock** (`dash-to-dock@micxgx.gmail.com`)
* **Dash to Panel** (`dash-to-panel@jderose9.github.com`)
* **Extend Left Box 2** (`extend-left-box2@linuxdeepin.com`)
* **Extension List** (`extension-list@tu.berry`)
* **Fly-Pie** (`flypie@schneegans.github.com`)
* **GSConnect** (`gsconnect@andyholmes.github.io`)
* **GTK4 Desktop Icons NG** (`gtk4-ding@smedius.gitlab.com`)
* **Just Perfection** (`just-perfection-desktop@just-perfection`)
* **Kando Integration** (`kando-integration@kando-menu.github.io`)
* **Lockscreen Extension** (`lockscreen-extension@pratap.fastmail.fm`)
* **Logo Menu** (`logomenu@aryan_k`)
* **OpenBar** (`openbar@neuromorph`)
* **OpenRGB Sync Accent Color** (`openrgb-sync-accent-color@evertonstz.github.io`)
* **Snowy** (`snowy@exposedcat`)
* **User Themes** (`user-theme@gnome-shell-extensions.gcampax.github.com` & `user-theme-x@tuberry.github.io`)
* **Vitals** (`Vitals@CoreCoding.com`)
* **Wiggle** (`wiggle@mechtifs`)
* **Window Animations** (`window-animations@rliang.github.com`)
